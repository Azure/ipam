/**
 * ConfigureGrid - Lightweight grid for configuration panels
 *
 * A simple AG Grid wrapper for grids that don't need header menus
 * or view persistence. Used by block.jsx, space.jsx, etc.
 */

export { default } from './ConfigureGrid';
export { default as ConfigureGrid } from './ConfigureGrid';
