name: Azure IPAM Build

run-name: Azure IPAM Container Build

on:
  release:
    types: [ published ]

permissions:
  id-token: write

env:
  ACR_NAME: ${{ vars.IPAM_PROD_ACR }}

jobs:
  init:
    name: Initialize Azure IPAM Build
    runs-on: ubuntu-latest
    environment: production
    outputs:
      ipamVersion: ${{ steps.extractVersion.outputs.ipamVersion }}
    steps:
      - run: echo "Job triggered by a ${{ github.event_name }} event."
      - run: echo "Release Tag - ${{ github.event.release.tag_name }}."

      - name: Extract IPAM Version
        id: extractVersion
        env:
          releaseTag: ${{ github.event.release.tag_name }}
        shell: pwsh
        run: |
          $version = $env:releaseTag -replace "^v", ""
          Write-Output "ipamVersion=$version" >> $Env:GITHUB_OUTPUT

  build:
    name: Build Azure IPAM Containers
    runs-on: ubuntu-latest
    environment: production
    needs: [ init ]
    steps:
      - name: Azure Login
        id: azureLogin
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          enable-AzPSSession: true

      - name: Checkout Azure IPAM Code
        id: checkoutRepo
        uses: actions/checkout@v6
        with:
          sparse-checkout: |
            engine
            ui

      - name: Build Azure IPAM Containers
        id: buildContainers
        env:
          IPAM_VERSION: ${{ needs.init.outputs.ipamVersion }}
        run: |
          az acr build -r $ACR_NAME -t ipam:$IPAM_VERSION -t ipam:latest --build-arg IPAM_VERSION=$IPAM_VERSION -f ./Dockerfile.deb .
          az acr build -r $ACR_NAME -t ipamfunc:$IPAM_VERSION -t ipamfunc:latest --build-arg IPAM_VERSION=$IPAM_VERSION -f ./Dockerfile.func .

  build-legacy:
    name: Build Legacy Azure IPAM Containers
    runs-on: ubuntu-latest
    environment: production
    needs: [ init ]
    steps:
      - name: Azure Login
        id: azureLogin
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          enable-AzPSSession: true

      - name: Checkout Azure IPAM Code
        id: checkoutRepo
        uses: actions/checkout@v6
        with:
          sparse-checkout: |
            engine
            ui
            lb

      - name: Build Legacy Azure IPAM Containers
        id: buildLegacyContainers
        env:
          IPAM_VERSION: ${{ needs.init.outputs.ipamVersion }}
        run: |
          az acr build -r $ACR_NAME -t ipam-engine:$IPAM_VERSION -t ipam-engine:latest --build-arg IPAM_VERSION=$IPAM_VERSION -f ./engine/Dockerfile.deb ./engine
          az acr build -r $ACR_NAME -t ipam-func:$IPAM_VERSION -t ipam-func:latest --build-arg IPAM_VERSION=$IPAM_VERSION -f ./engine/Dockerfile.func ./engine
          az acr build -r $ACR_NAME -t ipam-ui:$IPAM_VERSION -t ipam-ui:latest --build-arg IPAM_VERSION=$IPAM_VERSION -f ./ui/Dockerfile.deb ./ui
          az acr build -r $ACR_NAME -t ipam-lb:$IPAM_VERSION -t ipam-lb:latest --build-arg IPAM_VERSION=$IPAM_VERSION -f ./lb/Dockerfile ./lb
